---
import Base from "../../layouts/Base.astro";

const title = "Contáctanos | ByteWorks - Construyamos Tu Sitio Web";
const description =
  "Ponte en contacto con ByteWorks para iniciar tu proyecto de diseño web. Contáctanos a través de nuestro formulario o redes sociales para discutir tus ideas y obtener una cotización.";
const breadcrumb = [
  { name: "Inicio", url: "https://byteworksagency.com/es/" },
  { name: "Contacto", url: "https://byteworksagency.com/es/contacto/" },
];

const phone = (import.meta.env || {}).PUBLIC_WHATSAPP_PHONE || "";
const message =
  (import.meta.env || {}).PUBLIC_WHATSAPP_MSG ||
  "Hola ByteWorks, estoy interesado en un sitio web.";
const wa = phone
  ? `https://wa.me/${phone}?text=${encodeURIComponent(message)}`
  : null;
---

<Base
  title={title}
  description={description}
  image="/og-image.png"
  lang="es"
  breadcrumb={breadcrumb}
>
  <section
    class="px-6 md:px-10 lg:px-16 max-w-3xl mx-auto py-16 md:py-24 text-center space-y-8"
    data-wa={phone || ""}
  >
    <h1 class="text-3xl md:text-5xl font-semibold">Trabajemos juntos</h1>
    <p class="text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
      ¿Listo para una web moderna, bilingüe y con mantenimiento incluido? Elige
      cualquiera de nuestras redes en la sección “Síguenos” (WhatsApp,
      Instagram, etc.). El formulario nos ayuda a responderte rápido por el
      canal que prefieras.
    </p>

    <form id="contact-form" class="mt-12 text-left space-y-5 max-w-md mx-auto">
      <div>
        <label for="name" class="block text-sm font-medium mb-1"
          >Nombre / Marca / Empresa / Organización</label
        >
        <input
          id="name"
          name="name"
          type="text"
          required
          placeholder="ej. Juan Pérez / ACME S.A."
          class="w-full rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-black px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-black dark:focus:ring-white"
        />
      </div>

      <div id="phone_wrap" class="">
        <label for="phone" class="block text-sm font-medium mb-1"
          >Contacto preferido (WhatsApp o usuario social)</label
        >
        <input
          id="phone"
          name="phone"
          type="tel"
          inputmode="tel"
          placeholder="ej. @tuMarca o +1 868 775 9858"
          class="w-full rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-black px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-black dark:focus:ring-white"
        />
      </div>

      <div>
        <label for="email" class="block text-sm font-medium mb-1"
          >Correo electrónico</label
        >
        <input
          id="email"
          name="email"
          type="email"
          required
          placeholder="ej. contacto@empresa.com"
          class="w-full rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-black px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-black dark:focus:ring-white"
        />
      </div>

      <div>
        <label for="message" class="block text-sm font-medium mb-1"
          >Mensaje</label
        >
        <textarea
          id="message"
          name="message"
          rows="5"
          required
          placeholder="Cuéntanos un poco sobre tu proyecto..."
          class="w-full rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-black px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-black dark:focus:ring-white"
        ></textarea>
      </div>

      <!-- Campos honeypot - ocultos para usuarios, verificados al enviar -->
      <input
        type="text"
        id="website"
        name="website"
        tabindex="-1"
        autocomplete="off"
        style="position:absolute;left:-10000px;top:auto;width:1px;height:1px;overflow:hidden;"
        aria-hidden="true"
      />
      <input
        type="url"
        id="company_url"
        name="company_url"
        tabindex="-1"
        autocomplete="off"
        style="opacity:0;position:absolute;top:0;left:0;height:0;width:0;z-index:-1;pointer-events:none;"
        aria-hidden="true"
      />

      <button
        id="submitBtn"
        type="submit"
        class="w-full md:w-auto inline-flex items-center justify-center px-6 py-3 rounded-md bg-black text-white dark:bg-white dark:text-black hover:opacity-90 transition"
      >
        Enviar mensaje
      </button>

      <p
        id="formStatus"
        role="status"
        aria-live="polite"
        class="text-sm text-gray-600 dark:text-gray-300 min-h-[1.5rem]"
        data-tone="idle"
      >
      </p>
    </form>

    <div
      class="border-t border-gray-200 dark:border-gray-800 pt-10 mt-10 text-sm text-gray-600 dark:text-gray-400"
    >
      <p>
        ¿Prefieres llamada o reunión? La agendamos después de un breve chat.
      </p>
      <p class="mt-2 text-xs text-gray-500">
        Horario: Lunes a Viernes, 9:00 AM - 6:00 PM (AST)
      </p>
    </div>
  </section>

  <script>
    (function () {
      // Type-safe element getters with proper type guards
      const form = document.getElementById("contact-form");
      if (!(form instanceof HTMLFormElement)) return;

      const nameEl = document.getElementById("name");
      const emailEl = document.getElementById("email");
      const phoneEl = document.getElementById("phone");
      const messageEl = document.getElementById("message");
      const hpEl = document.getElementById("website");
      const hp2El = document.getElementById("company_url");
      const btn = document.getElementById("submitBtn");
      const statusEl = document.getElementById("formStatus");

      if (!(nameEl instanceof HTMLInputElement)) return;
      if (!(emailEl instanceof HTMLInputElement)) return;
      if (!(phoneEl instanceof HTMLInputElement)) return;
      if (!(messageEl instanceof HTMLTextAreaElement)) return;
      if (!(hpEl instanceof HTMLInputElement)) return;
      if (!(hp2El instanceof HTMLInputElement)) return;
      if (!(btn instanceof HTMLButtonElement)) return;
      if (!(statusEl instanceof HTMLParagraphElement)) return;

      // Email validation regex
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

      // Validation state classes
      const validClass = "border-green-500 dark:border-green-400";
      const invalidClass = "border-red-500 dark:border-red-400";

      function setFieldState(
        el: HTMLInputElement | HTMLTextAreaElement,
        isValid: boolean | null,
      ) {
        el.classList.remove(validClass, invalidClass);
        if (isValid === true) {
          el.classList.add(validClass);
        } else if (isValid === false) {
          el.classList.add(invalidClass);
        }
      }

      function validateField(
        el: HTMLInputElement | HTMLTextAreaElement,
        pattern?: RegExp,
      ): boolean {
        const value = el.value.trim();
        if (!value) {
          setFieldState(el, null);
          return false;
        }
        const isValid = pattern ? pattern.test(value) : value.length > 0;
        setFieldState(el, isValid);
        return isValid;
      }

      // Real-time validation on blur
      nameEl.addEventListener("blur", () => validateField(nameEl));
      emailEl.addEventListener("blur", () =>
        validateField(emailEl, emailRegex),
      );
      phoneEl.addEventListener("blur", () => validateField(phoneEl));
      messageEl.addEventListener("blur", () => validateField(messageEl));

      // Also validate on input for immediate feedback
      nameEl.addEventListener("input", () => {
        if (nameEl.value) validateField(nameEl);
      });
      emailEl.addEventListener("input", () => {
        if (emailEl.value) validateField(emailEl, emailRegex);
      });
      phoneEl.addEventListener("input", () => {
        if (phoneEl.value) validateField(phoneEl);
      });
      messageEl.addEventListener("input", () => {
        if (messageEl.value) validateField(messageEl);
      });

      function setStatus(message: string, tone = "info") {
        statusEl.textContent = message;
        statusEl.dataset.tone = tone;
        statusEl.classList.remove(
          "text-red-600",
          "dark:text-red-400",
          "text-green-600",
          "dark:text-green-400",
          "text-amber-600",
          "dark:text-amber-400",
        );

        if (!message) {
          statusEl.dataset.tone = "idle";
          return;
        }

        const toneClasses =
          tone === "error"
            ? ["text-red-600", "dark:text-red-400"]
            : tone === "success"
              ? ["text-green-600", "dark:text-green-400"]
              : ["text-amber-600", "dark:text-amber-400"];

        statusEl.classList.add(...toneClasses);
      }

      form.addEventListener("submit", async (ev) => {
        ev.preventDefault();

        const name = nameEl.value.trim();
        const email = emailEl.value.trim();
        const phone = phoneEl.value.trim();
        const msg = messageEl.value.trim();

        // Check both honeypot fields
        if (hpEl.value.trim() || hp2El.value.trim()) return;

        // Validate all fields
        const nameValid = validateField(nameEl);
        const emailValid = validateField(emailEl, emailRegex);
        const phoneValid = validateField(phoneEl);
        const msgValid = validateField(messageEl);

        if (!nameValid || !emailValid || !phoneValid || !msgValid) {
          setStatus(
            "Por favor completa todos los campos correctamente.",
            "error",
          );
          return;
        }

        const original = btn.textContent;
        setStatus("Enviando...", "info");
        btn.disabled = true;
        btn.textContent = "Enviando...";

        try {
          const res = await fetch("/api/contact", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ name, email, phone, message: msg }),
          });

          if (!res.ok) {
            const errorData = await res.json();
            throw new Error(
              errorData.message || errorData.error || "Failed to send",
            );
          }

          // Clear form and validation states
          nameEl.value = "";
          emailEl.value = "";
          phoneEl.value = "";
          messageEl.value = "";
          hpEl.value = "";
          hp2El.value = "";
          [nameEl, emailEl, phoneEl, messageEl].forEach((el) =>
            setFieldState(el, null),
          );

          setStatus(
            "¡Gracias! Recibimos tu solicitud y te contactaremos pronto.",
            "success",
          );
        } catch (err) {
          console.error(err);
          const errorMsg =
            err instanceof Error ? err.message : "Error desconocido";
          setStatus(`Error: ${errorMsg}`, "error");
        } finally {
          btn.disabled = false;
          btn.textContent = original || "Enviar mensaje";
        }
      });
    })();
  </script>
</Base>
